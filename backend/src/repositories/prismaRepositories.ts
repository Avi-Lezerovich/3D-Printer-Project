// @ts-nocheck
// Minimal stub for Prisma repositories (no runtime dependency on @prisma/client).
// Replace with real implementation once persistence rollout is finalized.
import { Repositories } from './types.js'

export function createPrismaRepositories(): Repositories {
  throw new Error('Prisma repositories not enabled yet')
}
